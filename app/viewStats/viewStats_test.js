'use strict';

describe('myApp.viewStats module', function() {

  beforeEach(module('myApp.viewStats'));

  describe('viewStats controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewStatsCtrl = $controller('ViewStatsCtrl');
      expect(viewStatsCtrl).toBeDefined();
    }));

  });
});
