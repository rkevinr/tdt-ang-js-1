'use strict';

describe('myApp.viewExpenses module', function() {

  beforeEach(module('myApp.viewExpenses'));

  describe('viewExpenses controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewExpensesCtrl = $controller('ViewExpensesCtrl');
      expect(viewExpensesCtrl).toBeDefined();
    }));

  });
});
